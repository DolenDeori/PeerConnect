import { View, Text } from "react-native";
import React from "react";

const PackageHistory = () => {
  return (
    <View>
      <Text>PackageHistory</Text>
    </View>
  );
};

export default PackageHistory;
