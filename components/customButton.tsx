import { Text, TouchableOpacity } from "react-native";

const CustomButton = () => (
  <TouchableOpacity>
    <Text className="font-bold text-blue-500">Click Here</Text>
  </TouchableOpacity>
);

export default CustomButton;
